import React from 'react';
import { Link } from 'react-router-dom';

const RessionFront = () => {
      return (
            <div>
                  Welcome to the Ression....

                  <Link className='btn btn-info' to='/ression/register'>Register now </Link>
            </div>
      );
};

export default RessionFront;